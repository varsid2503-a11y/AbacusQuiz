{
  "name": "abacus-quiz",
  "private": true,
  "workspaces": ["server"],
  "scripts": {
    "dev": "npm run dev -w server",
    "build": "npm run build -w server",
    "start": "npm run start -w server"
  }
}
